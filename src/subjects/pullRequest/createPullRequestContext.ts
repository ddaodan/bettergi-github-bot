import type { PullRequestContext } from "../../core/types.js";

export function createPullRequestContext(): PullRequestContext {
  throw new Error("Pull request handling is reserved for a future version.");
}
